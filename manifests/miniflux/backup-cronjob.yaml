---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: backup
  namespace: miniflux
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: backup
  namespace: miniflux
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames: ["miniflux-secrets"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: backup
  namespace: miniflux
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: backup
subjects:
  - kind: ServiceAccount
    name: backup
    namespace: miniflux
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: miniflux-backup
  namespace: miniflux
spec:
  schedule: "0 1 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      activeDeadlineSeconds: 3600
      backoffLimit: 2
      template:
        spec:
          serviceAccountName: backup
          restartPolicy: OnFailure
          containers:
            - name: backup
              image: postgres:18
              command:
                - /bin/bash
                - -c
                - |
                  set -euo pipefail
                  STAMP=$(date +%Y%m%d-%H%M%S)
                  echo "Starting miniflux backup ${STAMP}..."

                  pg_dump --host miniflux-postgres --username miniflux --dbname miniflux \
                    | gzip > /backup/miniflux-db-${STAMP}.sql.gz
                  echo "Database dump complete."

                  find /backup -name "miniflux-db-*.sql.gz" -mtime +7 -delete
                  echo "Retention cleanup complete."
              env:
                - name: PGPASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: miniflux-secrets
                      key: database-password
              volumeMounts:
                - name: backup
                  mountPath: /backup
              resources:
                requests:
                  memory: 64Mi
                  cpu: 50m
                limits:
                  memory: 256Mi
          volumes:
            - name: backup
              persistentVolumeClaim:
                claimName: miniflux-backup
